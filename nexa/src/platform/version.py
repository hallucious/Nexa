from __future__ import annotations

# Platform plugin API version for manifest compatibility checks.
# SemVer-like string; v0.1 corresponds to the existing hybrid in-tree plugin system.
PLATFORM_API_VERSION = "0.1"
