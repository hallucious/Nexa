config_id: summarize_plan
version: "1.0"
provider_ref: ai
provider_inputs:
  prompt: node.structure_node.output.text
output_mapping:
  text: provider.ai.output
runtime_config:
  return_raw_output: true
  write_observability: true
