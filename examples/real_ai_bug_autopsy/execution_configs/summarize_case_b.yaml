config_id: summarize_case_b
version: "1.0"
provider_ref: ai
provider_inputs:
  prompt: input.prompt_b
output_mapping:
  output: provider.ai.output
runtime_config:
  return_raw_output: true
  write_observability: true
